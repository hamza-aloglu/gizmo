version: '3.8'
services:
  authserver:
    image: hamzul/authserver:0.1
    ports:
      - "8080:8080"
    environment:
      - SERVER_PORT=8080
      - POSTGRES_URL=${POSTGRES_URL}
      - POSTGRES_USERNAME=${POSTGRES_USERNAME}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - ISSUER=http://authserver:8080

  resourceserver:
    image: hamzul/resourceserver:0.1
    ports:
      - "8081:8081"
    environment:
      - SERVER_PORT=8081
      - POSTGRES_URL=${POSTGRES_URL}
      - POSTGRES_USERNAME=${POSTGRES_USERNAME}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - ISSUER_URI=http://authserver:8080
    depends_on:
      - authserver